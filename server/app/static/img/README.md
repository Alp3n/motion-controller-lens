# Logo

Wrzuć tu plik `logo.png` (albo `logo.svg` — wtedy zmień rozszerzenie
w `src` w `server/app/static/index.html`) — nagłówek panelu operatora
podepnie go automatycznie, bez zmian w kodzie. Dopóki pliku nie ma, nagłówek
wygląda normalnie (bez połamanej ikonki) — `onerror` w HTML go usuwa.
